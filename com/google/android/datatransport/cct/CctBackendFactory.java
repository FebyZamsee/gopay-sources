package com.google.android.datatransport.cct;

import o.ActionBar;
import o.hide;
import o.setTitle;

public class CctBackendFactory implements setTitle {
    public ActionBar.NavigationMode create(ActionBar.LayoutParams layoutParams) {
        return new hide(layoutParams.read(), layoutParams.onTransact(), layoutParams.asInterface());
    }
}
